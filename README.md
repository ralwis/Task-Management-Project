🧩 Task Management System
This project is a Task Management Application built to manage users and tasks efficiently. It provides functionalities such as user authentication, task creation, updating, deletion, 
and data management features like sorting, filtering, and pagination.

⚙️ Database Setup
To create the required database tables and populate them with sample data, simply run the attached script file: 'Task Management Create Populate Scripts.sql'

This script will automatically:

Create all necessary database tables.
Insert initial data for testing and demonstration purposes.

🔐 Authentication
The application uses a simple username/password-based authentication (no JWT implementation).
You can log in using the following default credentials:

Email: admin@gmail.com
Password: admin123

Alternatively, you can create a new user and use those credentials to log in to the system.

🚀 Application Features
The system provides the following key features:

👤 User Management
Create new users.
User login and authentication.

✅ Task Management
Create new tasks.
View all created tasks.
Update existing tasks.
Delete tasks.

🔍 Task Operations
Sorting tasks by column values.
Filtering tasks by keywords or status.
Pagination for easy navigation.
